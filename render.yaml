services:
  - type: web
    name: pdf-redirect-debug
    env: python
    plan: free
    autoDeploy: true
    buildCommand: 'pip install -r tools/pdf-debug/requirements.txt'
    startCommand: 'python tools/pdf-debug/pdf_redirect_server.py --port $PORT'
